<!doctype html>
<html>
<head>
  <meta charset="utf-8"/>
  <title>Certificate</title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      text-align: center;
      background: #f0f8ff;
      margin: 0;
      padding: 0;
      height: 100%;
      width: 100%;
    }

    .certificate-container {
  width: 1000px;
  height: 650px;   /* reduced height */
  margin: 20px auto;
  padding: 30px 40px;    /* reduced padding */
  background: white;
  border: 12px solid #2b6cb0;
  border-radius: 12px;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  box-sizing: border-box;       /* ensures nothing spills out */
}

    .header-logo img {
      width: 120px;
      height: 80px;
      object-fit: contain;
      margin-bottom: 10px;
    }

    .certificate-title {
      font-size: 32px;
      font-weight: bold;
      color: #2b6cb0;
      margin-top: 10px;
      margin-bottom: 20px;
    }

    .certificate-body {
      flex-grow: 1;
      display: flex;
      flex-direction: column;
      justify-content: center;
      padding: 10px 0;
    }

    .certificate-name {
      font-size: 40px;
      font-weight: bold;
      margin: 15px 0;
    }

    .certificate-text {
      font-size: 18px;
      color: #333;
      margin-bottom: 20px;
    }

    .footer {
      display: flex;
      justify-content: space-between;
      align-items: center;    /* align items vertically inside footer */
  margin-top: 10px;       /* reduce spacing */
  flex-shrink: 0; 
    }

    .footer-left {
      text-align: left;
      color: #333;
    }

    .footer-left p {
      margin: 0;
    }

    .footer-left .name {
      font-weight: bold;
      font-size: 18px;
    }

    .footer-left .title {
      font-size: 14px;
      margin-top: 3px;
    }

    .footer-right img {
      width: 70px;
      height: 70px;
      object-fit: contain;
    }
  </style>
</head>
<body>
  <div class="certificate-container">

    <!-- Top logo -->
    <div class="header-logo">
      <a href="https://risidio.com">
        <img src="{{logo}}" alt="Risidio Logo">
      </a>
    </div>

    <!-- Main content -->
    <div class="certificate-body">
      <h1>AI FIRST ACADEMY</h1>
      <div class="certificate-title">Certificate of Completion</div>
      <div>This is to certify that</div>
      <div class="certificate-name">{{name}}</div>
      <div class="certificate-text">
        has successfully completed the <strong>{{track}}</strong> program.
      </div>
      <div class="certificate-text">
        Issued by {{issued_by}} on {{issue_date}}
      </div>
      <div class="certificate-text" style="font-size:12px; color:#666;">
        Certificate ID: {{certificate_id}}
      </div>
    </div>

    <!-- Footer -->
    <div class="footer">
      <div class="footer-left">
        <p class="name">Dr Sarah Chen</p>
        <p class="title">Academy Director</p>
      </div>
      <div class="footer-right">
        <img src="{{verifiedLogo}}" alt="Verified Achievement">
      </div>
    </div>
  </div>
</body>
</html>

